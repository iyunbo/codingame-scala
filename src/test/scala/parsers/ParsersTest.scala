package org.iyunbo.coding
package parsers

import Parsers._

import org.scalatest.flatspec.AnyFlatSpec
import org.scalatest.matchers.should


class ParsersTest extends AnyFlatSpec with should.Matchers {
  it should "parse a single char" in {

//    decode(char('a'))("a") should be("a")
//    TODO
//    val decoded: List[String] = decode(listOfN(3, "ab" | "xyz" | "hij"))("abhijxyz").getOrElse(List())

  }

}
